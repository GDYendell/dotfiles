# Allow adding ssh keys on demand rather than on login
# Requires ssh-agent to be started, but not ssh-add
AddKeysToAgent yes

Host pc0118
  HostName pc0118
  ProxyJump mef65357@ssh.diamond.ac.uk
  User mef65357

Host *
  ControlMaster auto
  ControlPath ~/.ssh/ssh_mux_%h_%p_%r
